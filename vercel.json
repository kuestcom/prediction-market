{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run db:push && next build",
  "ignoreCommand": "git log -1 --pretty=%B | grep -q '\\[skip deploy\\]' && exit 0 || exit 1",
  "git": {
    "deploymentEnabled": {
      "main": true,
      "*": false
    }
  }
}
