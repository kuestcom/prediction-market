---
title: 'Get marks history'
openapi: 'GET /marks/history'
---

Returns finalized mark windows for one instrument.

`instrument` is required. Use optional `interval`, `from`, `to`, and `limit` query parameters to scope the result.
