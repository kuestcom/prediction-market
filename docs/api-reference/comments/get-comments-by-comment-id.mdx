---
title: Get Comments By Comment Id
description: "Returns replies for a parent comment."
_openapi:
  method: GET
---
List replies for a root comment. Pagination uses `limit` and `offset`.

## Playground

<APIPage
  document="community"
  operations={[
    { path: '/comments/{id}/replies', method: 'get' }
  ]}
/>
