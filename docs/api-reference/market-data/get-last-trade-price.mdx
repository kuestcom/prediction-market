---
title: 'Get last trade price'
openapi: 'GET /last-trade-price'
---


Fetch the most recent executed trade price for a single `token_id`.

The response includes the price and the side of the last aggressor.